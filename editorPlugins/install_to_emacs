##INSTALL TO EMACS##

Add this to your local ~/.emacs file (if file doesn't exist, create it)
Replace path to the script location of emacs_vorema.el

Compile a single jar file of Voremo, using sbt assembly

(add-to-list 'load-path "/home/koen/vorema/editorPlugins/")
(load "emacs_vorema.el")
